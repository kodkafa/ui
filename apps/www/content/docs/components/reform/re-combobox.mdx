---
title: ReCombobox
description: A wrapper for multiple elements.
component: true
---

<ComponentPreview
  name="re-combobox-demo"
  className="[&_input]:max-w-xs [&_button]:max-w-xs [&_div.reform-input-group]:max-w-xs"
  description="A form combobox component."
/>

## Installation

<Tabs defaultValue="cli">
<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx kodkafa@latest add re-combobox
```

</TabsContent>
<TabsContent value="manual">
  <Steps>
    <Step>Copy and paste the following code into your project.</Step>
      <ComponentSource name="re-combobox" />
    <Step>Update the import paths to match your project setup.</Step>
  </Steps>
</TabsContent>
</Tabs>

#### Dependencies

The ReCombobox is built using the `<Combobox />` from [shadcn/ui](https://ui.shadcn.com/docs/components/combobox).

See installation instructions for the [Combobox](/docs/components/combobox#installation) component.
