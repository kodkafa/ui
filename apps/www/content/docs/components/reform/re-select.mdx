---
title: ReSelect
description: A wrapper for multiple elements.
component: true
---

<ComponentPreview
  name="re-select-demo"
  className="[&_input]:max-w-xs [&_button]:max-w-xs [&_div.reform-input-group]:max-w-xs"
  description="A form select component."
/>

## Installation

<Tabs defaultValue="cli">
<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx kodkafa@latest add re-select
```

</TabsContent>
<TabsContent value="manual">
  <Steps>
    <Step>Copy and paste the following code into your project.</Step>
      <ComponentSource name="re-select" />
    <Step>Update the import paths to match your project setup.</Step>
  </Steps>
</TabsContent>
</Tabs>

#### Dependencies

The ReSelect is built using the `<Select />` from [shadcn/ui](https://ui.shadcn.com/docs/components/select).

See installation instructions for the [Select](/docs/components/select#installation) component.
